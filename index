<!DOCTYPE html>
<html lang="uz">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Shuxratjon — Marketing | Ro‘yxatdan o‘tish</title>
  <meta name="description" content="Shuxratjon — 4 yillik marketing tajribasi, 1 mln Telegram auditoriya, CodesMe asoschisi. Standart va Pro kurs toifalari. Netlify form orqali ro‘yxatdan o‘ting." />

  <style>
    :root{
      --primary:#ef5123;
      --primary-600:#d3471f;
      --primary-700:#b63e1b;
      --primary-300:#ff7a54;
      --text:#1c1c1c;
      --muted:#5b5b5b;
      --bg:#fff9f7;
      --card:#ffffff;
      --border:#f1e7e4;
      --focus:#ffb299;
      --success:#1a9b61;
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0}
    body{
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
      color:var(--text);
      background:var(--bg);
      line-height:1.5;
    }
    a{color:var(--primary);text-decoration:none}
    a:hover{text-decoration:underline}
    .container{
      width:100%;
      max-width:1100px;
      margin-inline:auto;
      padding: 0 20px;
    }

    /* Hero */
    .hero{
      background: radial-gradient(1200px 400px at 10% -10%, #ffddd2 0%, transparent 60%) ,
                  linear-gradient(135deg, var(--primary) 0%, var(--primary-600) 100%);
      color:#ffffff;
      padding:56px 0 40px;
    }
    .brand{
      display:flex;align-items:center;gap:12px;margin-bottom:28px
    }
    .logo{
      width:40px;height:40px;border-radius:10px;background:#fff2ee;
      display:grid;place-items:center;color:var(--primary);font-weight:800
    }
    .hero h1{
      margin:0 0 10px 0;font-size: clamp(28px, 4vw, 40px);
      letter-spacing:0.2px
    }
    .hero p.sub{
      margin:0;color:#ffeae3;font-size:clamp(15px, 2.4vw, 18px)
    }

    /* Info grid */
    .info{
      display:grid;gap:16px;margin-top:28px
    }
    .chip{
      display:inline-flex;align-items:center;gap:8px;
      background:#fff2ee;color:#6f2a19;border:1px solid #ffd7cd;
      padding:8px 12px;border-radius:999px;font-size:14px;width:max-content
    }

    /* Sections */
    .section{
      padding:28px 0
    }
    .cards{
      display:grid;gap:16px;
      grid-template-columns: repeat(2, minmax(0,1fr));
    }
    @media (max-width: 760px){
      .cards{grid-template-columns: 1fr}
    }
    .card{
      background:var(--card);border:1px solid var(--border);
      border-radius:16px;padding:20px
    }
    .card h3{margin:0 0 8px 0;font-size:20px}
    .muted{color:var(--muted);font-size:14px}

    /* Pricing */
    .price{
      font-weight:800;font-size:26px;margin: 10px 0;color:var(--primary-700)
    }
    .badge{
      display:inline-block;background: #fff2ee;color:#6f2a19;
      border:1px solid #ffd7cd;border-radius:999px;padding:6px 10px;font-size:12px
    }

    /* Form */
    .form-wrap{
      background:#fff;border:1px solid var(--border);border-radius:16px;padding:20px
    }
    form{
      display:grid;gap:14px
    }
    label{font-weight:600;font-size:14px;margin-bottom:6px;display:block}
    .field{display:flex;flex-direction:column}
    input, select, textarea{
      width:100%;background:#fff;border:1px solid #eadedb;border-radius:10px;
      padding:12px 12px;font-size:16px;color:var(--text)
    }
    input::placeholder, textarea::placeholder{color:#a9a9a9}
    input:focus, select:focus, textarea:focus{
      outline:3px solid var(--focus);border-color: var(--primary-300)
    }
    .row{
      display:grid;gap:14px;grid-template-columns: repeat(2, minmax(0,1fr))
    }
    @media (max-width: 640px){ .row{grid-template-columns:1fr} }
    .help{font-size:12px;color:var(--muted);margin-top:6px}
    .hidden{display:none !important}

    .submit{
      display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-top:6px
    }
    button{
      appearance:none;border:0;background:var(--primary);color:#fff;
      padding:12px 18px;border-radius:10px;font-weight:700;cursor:pointer;
      transition: transform .05s ease, background .2s ease
    }
    button:hover{background:var(--primary-600)}
    button:active{transform: translateY(1px)}
    .guarantee{font-size:13px;color:#6a6a6a}

    /* Footer */
    footer{
      margin-top:36px;padding:22px 0;border-top:1px solid var(--border);
      color:#4a4a4a;background:#fff
    }
    .footer-grid{
      display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap
    }
    .foot-link{color:var(--primary-700);font-weight:700}
  </style>
</head>
<body>
  <header class="hero">
    <div class="container">
      <div class="brand">
        <div class="logo">S</div>
        <strong>Shuxratjon</strong>
      </div>
      <h1>Marketing bo‘yicha natijaga yo‘naltirilgan o‘qituvchi</h1>
      <p class="sub">4 yillik tajriba • 1 mln Telegram auditoriya • CodesMe asoschisi</p>

      <div class="info">
        <span class="chip">Marketingda 4 yil tajriba</span>
        <span class="chip">Telegram auditoriya: 1 000 000+</span>
        <span class="chip">CodesMe platformasi asoschisi</span>
        <span class="chip">10 dan ortiq o‘quvchilar o‘qitilgan</span>
      </div>
    </div>
  </header>

  <main class="container">
    <section class="section">
      <div class="cards">
        <article class="card">
          <h3>Standart</h3>
          <p class="muted">Boshlang‘ichdan natijagacha — amaliy yo‘l xaritasi</p>
          <div class="price">499 000 so‘m</div>
          <span class="badge">O‘quvchilar uchun mos</span>
        </article>
        <article class="card">
          <h3>Pro</h3>
          <p class="muted">Kengaytirilgan modul va shaxsiy yo‘riqnoma</p>
          <div class="price">999 000 so‘m</div>
          <span class="badge">Professionallar uchun mos</span>
        </article>
      </div>
    </section>

    <section class="section">
      <div class="form-wrap">
        <h2 style="margin:0 0 10px 0">Ro‘yxatdan o‘tish</h2>
        <p class="muted" style="margin:0 0 14px 0">Quyidagi formani to‘ldiring — siz bilan tez orada bog‘lanamiz.</p>

        <!-- Netlify form -->
        <form name="royxatdan-otish" method="POST" data-netlify="true" netlify-honeypot="bot-field" action="/success.html">
          <input type="hidden" name="form-name" value="royxatdan-otish" />
          <p class="hidden">
            <label>Bot maydoni: <input name="bot-field" /></label>
          </p>

          <div class="row">
            <div class="field">
              <label for="fullname">To‘liq ism</label>
              <input id="fullname" name="fullname" type="text" placeholder="Ismingiz" required />
            </div>
            <div class="field">
              <label for="phone">Telefon</label>
              <input id="phone" name="phone" type="tel" inputmode="tel" placeholder="90 749 0870" required />
              <div class="help">Aloqa uchun faol raqam kiriting</div>
            </div>
          </div>

          <div class="row">
            <div class="field">
              <label for="telegram">Telegram</label>
              <input id="telegram" name="telegram" type="text" placeholder="@username" required />
            </div>
            <div class="field">
              <label for="email">Email (ixtiyoriy)</label>
              <input id="email" name="email" type="email" placeholder="email@example.com" />
            </div>
          </div>

          <div class="field">
            <label for="plan">Kurs toifasi</label>
            <select id="plan" name="plan" required>
              <option value="" disabled selected>Tanlang</option>
              <option value="Standart">Standart — 499 000 so‘m</option>
              <option value="Pro">Pro — 999 000 so‘m</option>
            </select>
          </div>

          <div class="field">
            <label for="goal">Maqsad (ixtiyoriy)</label>
            <textarea id="goal" name="goal" rows="3" placeholder="Qisqacha maqsadingizni yozing"></textarea>
          </div>

          <div class="submit">
            <button type="submit">Yuborish</button>
            <span class="guarantee">Ma’lumotlar xavfsiz saqlanadi.</span>
          </div>
        </form>
      </div>
    </section>
  </main>

  <footer>
    <div class="container footer-grid">
      <div>sayt: <a class="foot-link" href="https://zakeeroo.uz" target="_blank" rel="noopener">zakeeroo.uz</a></div>
      <div>tel: <a class="foot-link" href="tel:+998907490870">90 749 0870</a></div>
    </div>
  </footer>
</body>
</html>
<!DOCTYPE html>
<html lang="uz">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Rahmat!</title>
  <style>
    :root{
      --primary:#ef5123;
      --primary-600:#d3471f;
      --border:#f1e7e4;
      --text:#1c1c1c;
      --bg:#fff9f7;
    }
    html,body{margin:0;padding:0}
    body{
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, "Helvetica Neue", Arial;
      background:var(--bg);color:var(--text)
    }
    .wrap{
      min-height:100dvh;display:grid;place-items:center;padding:20px
    }
    .card{
      width:100%;max-width:640px;background:#fff;border:1px solid var(--border);
      border-radius:16px;padding:28px;text-align:center
    }
    h1{margin:0 0 10px 0;color:var(--primary)}
    p{margin:0 0 16px 0}
    a.btn{
      display:inline-block;background:var(--primary);color:#fff;
      padding:12px 18px;border-radius:10px;font-weight:700;text-decoration:none
    }
    a.btn:hover{background:#d3471f}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>Ma'lumotlaringiz qabul qilindi.</h1>
      <p>Tez orada sizga bog'lanamiz!</p>
      <a class="btn" href="/">Bosh sahifaga qaytish</a>
    </div>
  </div>
</body>
</html>
